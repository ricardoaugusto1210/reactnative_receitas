import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    padding: 8,
    marginBottom: 14,
  },
  name: {
    fontWeight: 'bold',
    fontSize: 18,
  },
  text: {
    lineHeight: 20,
  }
});